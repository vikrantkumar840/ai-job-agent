apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-job-agent
data:
  # --- Postgres ---
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "aijobagent"

  # --- MongoDB ---
  MONGODB_DATABASE: "aijobagent"

  # --- Qdrant ---
  QDRANT_HOST: "qdrant"
  QDRANT_PORT: "6333"

  # --- JWT ---
  JWT_ALGORITHM: "HS256"
  JWT_ACCESS_TOKEN_EXPIRE_MINUTES: "60"

  # --- CORS / public URLs ---
  # CHANGE_AFTER_DOMAIN: replace with your real domain once purchased
  CORS_ALLOWED_ORIGINS: "https://YOUR_DOMAIN.com,https://www.YOUR_DOMAIN.com"
  NEXT_PUBLIC_API_URL: "https://api.YOUR_DOMAIN.com"
  API_URL: "https://api.YOUR_DOMAIN.com"
